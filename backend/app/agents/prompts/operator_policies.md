<!--
  Optional override: set OPERATOR_LLM_POLICY_PATH to a .md or .yaml file.

  Default operator policy for the Nemotron supervisor is the bundled policy_pack:
  security_software_llm_guard_policies.yaml (rendered to markdown at runtime).

  Authoritative allow/block/routing remains with OPA (infra/opa) and downstream gates.
-->
